@model CreateReferralTestHarness.Models.CreateReferral

@{
    ViewBag.Title = "TrackReferrals";
}

<h2>Track Referrals</h2>


@*@using ( Html.BeginForm( "Create", "CreateReferral", FormMethod.Post, new { @class = "form-horizontal", role = "form" } ) )*@
@using ( Html.BeginForm( "TrackReferral", "CreateReferral", FormMethod.Get, new { @class = "form-horizontal", role = "form" } ) )
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Enter AccountId:</h4>
        <hr />
        @Html.ValidationSummary( true, "", new { @class = "text-danger" } )
        <div class="form-group">
            @Html.LabelFor( model => model.AccountId, htmlAttributes: new { @class = "control-label col-md-2" } )
            <div class="col-md-10">
                @Html.EditorFor( model => model.AccountId, new { htmlAttributes = new { @class = "form-control" } } )
                @Html.ValidationMessageFor( model => model.AccountId, "", new { @class = "text-danger" } )
            </div>
        </div>

        @*<div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Create" class="btn btn-default" />
                </div>
            </div>*@
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Get" class="btn btn-default" />
            </div>
        </div>

        <p>
            @Html.ActionLink( "Home", "Index", "Home" )
        </p>
    </div>
}

@section Scripts {
    @Scripts.Render( "~/bundles/jqueryval" )
}